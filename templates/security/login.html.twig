{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}

<div class="container-fluid login">
    <ul class="nav nav-tabs">
      <li class="nav-item">
        <a class="nav-link active" data-toggle="tab" href="{{ path('app_login') }}">CONNEXION</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" data-toggle="tab" href="{{ path('app_register') }}">CRÉER UN COMPTE</a>
      </li>
    </ul>
</div>
<div class="container-fluid login-id">
    <div class="row w-80">
        <div class="col-md-7 border bg-light me-4">
            <div class="p-3">
                <form method="post">
                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                    <h5 class="mt-3">Connexion</h5>
                    <hr class="mb-3 mt-3">
                    <label for="inputEmail" class="mb-2"><span class="text-danger">*</span> E-mail</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="input-design" autocomplete="email" required autofocus>
                    <label for="inputPassword" class="mb-2 mt-3"><span class="text-danger">*</span> Mot de passe</label>
                    <input type="password" name="password" id="inputPassword" class="input-design" autocomplete="current-password" required>
                    <div class=" float-end mt-3">
                        <a href="{{ path('app_forgot_password_request') }}" class="mt-3">Mot de passe oublié ?</a>
                    </div>
                    <input type="hidden" name="_csrf_token"
                        value="{{ csrf_token('authenticate') }}"
                    >
                    <button class="mt-5 btn-connexion" type="submit">
                        CONNEXION
                    </button>
                </form>
            </div>
        </div>
        <div class="col-md-4 border bg-light ">
            <div class="p-3">
                <h5 class="mt-3">Voir vos commandes</h5>
                <hr class="mb-3 mt-3">
                <p>Vous pouvez consulter vos commandes même si vous n'avez pas de compte client. Pour cela, renseignez le numéro de la commande et le code postal de l'adresse de facturation utilisée.</p>
                
                <label for="commande" class="mb-2"><span class="text-danger">*</span> Numéro de commande</label>
                <input type="text" name="commande" id="commande" class="form-control" autocomplete="commande" required autofocus>
                <label for="email" class="mb-2 mt-3"><span class="text-danger">*</span> E-mail utilisé lors de la commande</label>
                <input type="email" name="email" class="form-control" autocomplete="email" required>
                <label for="codePostal" class="mb-2 mt-3"><span class="text-danger">*</span> Code postal de l'adresse de facturation utilisée</label>
                <input type="text" name="codePostal" class="form-control" autocomplete="codePostal" required>
            
                <button class="mt-5 btn-connexion" type="submit">
                    VOIR MES COMMANDES
                </button>
            </div>
        </div>
    </div>
</div>

{% endblock %}
