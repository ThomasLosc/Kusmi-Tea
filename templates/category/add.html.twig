{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row mb-4">
            <div class="col-12">
                {% for label, flashes in app.session.flashbag.all %}
                    {% for flash in flashes %}
                        {% if ( label == 'success' ) %}
                            <div class="alert alert-success" role="alert"><i class="fas fa-check"></i>
                                {{ flash }}
                            </div>
                        {% elseif ( label == 'error' ) %}
                            <div class="alert alert-danger" role="alert"><i class="fas fa-exclamation-circle"></i>
                                {{ flash }}
                            </div>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
            </div>
        </div>
        <div class="row">
            <div class="offset-md-2 col-md-8 border bg-light me-4">
                <div class="p-3">
                    {{ form_start(form) }}
                    <h5 class="mt-3">Créer une categorie</h5>
                    <hr class="mb-3 mt-3">
                    <label for="form_name" class="mb-2"><span class="text-danger">*</span> Nom de la catégorie</label>
                    {{ form_widget(form.name, {'attr': {'class': 'input-design'}}) }}<br>

                    <input type="hidden" name="_csrf_token"
                           value="{{ csrf_token('authenticate') }}"
                    >
                    <button class="mt-5" type="submit" style="background-color: #000;border: none;color: #fff;padding: 15px;width: 100%;">
                        CRÉER
                    </button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
